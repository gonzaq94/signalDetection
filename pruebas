from PIL import Image, ImageDraw, ImageTk
import numpy as np



im = Image.open("AIS_1_lb.png")
d = ImageDraw.Draw(im)
top = (150, 50)
left = (250, 50)
right = (250, 125)
down = (150,125)
line_color = (0, 0, 255)
d.line([top, left, right, down], fill=line_color, width=2)
im.save("drawn_grid.png")